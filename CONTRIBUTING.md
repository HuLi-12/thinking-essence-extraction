# Contributing

## 新增 Example 的规范

新增 example 必须完整包含以下 8 项：

1. **常见说法** —— 写出常见但不够本质的解释
2. **问题** —— 为什么这个解释是结果描述不是机制
3. **真正变化的变量** —— 直接变量、衍生变量、观测指标、隐藏变量
4. **本质一句话** —— 核心机制总结，不超过 3 句话
5. **机制链** —— 用 → 连接的完整因果链，不允许跳链
6. **代价交换** —— 以 X 为代价，换取 Y，风险是 Z
7. **可验证** —— 至少 1 个可证伪实验设计
8. **对当前模块的启发** —— 产出设计指导

## 禁止

- 只写概念解释而不给出变量表
- 只写结论而不给出机制链
- 没有可验证实验
- 把假设写成事实（必须标注 Evidence Level）
- 复用其他案例的结论而不迁移变量结构

## 输出风格

- 技术变量使用英文缩写首次出现时标注全称：如 ERF（Effective Receptive Field）
- 机制链用 → 连接，每步一个节点
- 代价交换用固定格式：`以 [X] 为代价，换取 [Y]，风险是 [Z]。`

## 新增文件规范

- 新增 example 放在 `examples/` 对应领域目录下
- 新增测试用例放在 `evals/test_prompts.md`，预期检查点同步更新到 `evals/expected_checkpoints.md`
- 新增变量卡片放在 `docs/variable_cards.md`
- 新增反例放在 `docs/anti_patterns.md`
- 新增脚本放在 `scripts/`，并确保 Python 3.8+ 兼容
